const admin = require('firebase-admin'); const schemes = require('../data/schemes.json'); const serviceAccount = require('../firebase.js'); admin.initializeApp({ credential: admin.credential.cert(serviceAccount) }); const db = admin.firestore(); async function uploadSchemes() { const batch = db.batch(); schemes.forEach(scheme => { const docRef = db.collection('schemes').doc(scheme.id); batch.set(docRef, scheme); }); await batch.commit(); console.log('Schemes successfully uploaded to Firestore!'); } uploadSchemes().catch(console.error);
